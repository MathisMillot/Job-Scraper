[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = --strict-config --strict-markers
markers =
    e2e: browser tests that require a Playwright browser
filterwarnings =
    error
